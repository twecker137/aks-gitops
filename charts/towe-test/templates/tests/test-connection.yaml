apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "towe-test.fullname" . }}-test-connection"
  labels:
    {{- include "towe-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "towe-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
