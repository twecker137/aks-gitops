apiVersion: v1
kind: Service
metadata:
  name: {{ include "towe-test.fullname" . }}
  labels:
    {{- include "towe-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "towe-test.selectorLabels" . | nindent 4 }}
